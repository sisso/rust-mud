{
  rooms-special: {
    initial: ${rooms.main.id},
  }

  rooms: {
    main: {
      id: 0,
      label: "Main Room",
      desc: "Main room where people born",
      exits.s: ${rooms.bar.id}
    }

    bar: {
      id: 1,
      label: "Bar",
      desc: "Main room where people born",
      exits.n: ${rooms.main.id}
      exits.s: ${rooms.florest.id}
    }

    florest: {
      id: 2,
      label: "Florest",
      desc: "A deep, ugly and dark florest.",
      exits.n: ${rooms.bar.id}
    }
  }
}
