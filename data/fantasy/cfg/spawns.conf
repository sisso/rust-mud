{
  spawns: {
    0: {
      room_id: ${rooms.bar.id}
      mob_id: ${mobs.drunk.id}
      max: 1
      delay.min: 5
      delay.max: 20
      trigger_on_start: true
    }
    1: {
      room_id: ${rooms.florest.id}
      mob_id: ${mobs.wolf.id}
      max: 4
      delay.min: 10
      delay.max: 30
      trigger_on_start: true
    }
  }
}
